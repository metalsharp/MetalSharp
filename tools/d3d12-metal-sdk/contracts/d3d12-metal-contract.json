{
  "contract_state": "reference_import",
  "data": {
    "format_map": {
      "DXGI_FORMAT_A8_UNORM": "MTLPixelFormatA8Unorm",
      "DXGI_FORMAT_B8G8R8A8_UNORM": "MTLPixelFormatBGRA8Unorm",
      "DXGI_FORMAT_B8G8R8A8_UNORM_SRGB": "MTLPixelFormatBGRA8Unorm_sRGB",
      "DXGI_FORMAT_BC1_UNORM": "MTLPixelFormatBC1_RGBA",
      "DXGI_FORMAT_BC1_UNORM_SRGB": "MTLPixelFormatBC1_RGBA_sRGB",
      "DXGI_FORMAT_BC2_UNORM": "MTLPixelFormatBC2_RGBA",
      "DXGI_FORMAT_BC2_UNORM_SRGB": "MTLPixelFormatBC2_RGBA_sRGB",
      "DXGI_FORMAT_BC3_UNORM": "MTLPixelFormatBC3_RGBA",
      "DXGI_FORMAT_BC3_UNORM_SRGB": "MTLPixelFormatBC3_RGBA_sRGB",
      "DXGI_FORMAT_BC4_SNORM": "MTLPixelFormatBC4_RSnorm",
      "DXGI_FORMAT_BC4_UNORM": "MTLPixelFormatBC4_RUnorm",
      "DXGI_FORMAT_BC5_SNORM": "MTLPixelFormatBC5_RGSnorm",
      "DXGI_FORMAT_BC5_UNORM": "MTLPixelFormatBC5_RGUnorm",
      "DXGI_FORMAT_BC6H_SF16": "MTLPixelFormatBC6H_RGBUfloat",
      "DXGI_FORMAT_BC6H_UF16": "MTLPixelFormatBC6H_RGBFloat",
      "DXGI_FORMAT_BC7_UNORM": "MTLPixelFormatBC7_RGBAUnorm",
      "DXGI_FORMAT_BC7_UNORM_SRGB": "MTLPixelFormatBC7_RGBAUnorm_sRGB",
      "DXGI_FORMAT_D16_UNORM": "MTLPixelFormatDepth16Unorm",
      "DXGI_FORMAT_D24_UNORM_S8_UINT": "MTLPixelFormatDepth24Unorm_Stencil8",
      "DXGI_FORMAT_D32_FLOAT": "MTLPixelFormatDepth32Float",
      "DXGI_FORMAT_D32_FLOAT_S8X24_UINT": "MTLPixelFormatDepth32Float_Stencil8",
      "DXGI_FORMAT_R10G10B10A2_UINT": "MTLPixelFormatRGB10A2Uint",
      "DXGI_FORMAT_R10G10B10A2_UNORM": "MTLPixelFormatRGB10A2Unorm",
      "DXGI_FORMAT_R11G11B10_FLOAT": "MTLPixelFormatRG11B10Float",
      "DXGI_FORMAT_R16G16B16A16_FLOAT": "MTLPixelFormatRGBA16Float",
      "DXGI_FORMAT_R16G16B16A16_SINT": "MTLPixelFormatRGBA16Sint",
      "DXGI_FORMAT_R16G16B16A16_SNORM": "MTLPixelFormatRGBA16Snorm",
      "DXGI_FORMAT_R16G16B16A16_UINT": "MTLPixelFormatRGBA16Uint",
      "DXGI_FORMAT_R16G16B16A16_UNORM": "MTLPixelFormatRGBA16Unorm",
      "DXGI_FORMAT_R16_FLOAT": "MTLPixelFormatR16Float",
      "DXGI_FORMAT_R16_SINT": "MTLPixelFormatR16Sint",
      "DXGI_FORMAT_R16_SNORM": "MTLPixelFormatR16Snorm",
      "DXGI_FORMAT_R16_UINT": "MTLPixelFormatR16Uint",
      "DXGI_FORMAT_R16_UNORM": "MTLPixelFormatR16Unorm",
      "DXGI_FORMAT_R32G32B32A32_FLOAT": "MTLPixelFormatRGBA32Float",
      "DXGI_FORMAT_R32G32B32A32_SINT": "MTLPixelFormatRGBA32Sint",
      "DXGI_FORMAT_R32G32B32A32_UINT": "MTLPixelFormatRGBA32Uint",
      "DXGI_FORMAT_R32G32B32_FLOAT": "MTLPixelFormatInvalid (no RGB32 in Metal, pad to RGBA32)",
      "DXGI_FORMAT_R32G32_FLOAT": "MTLPixelFormatRG32Float",
      "DXGI_FORMAT_R32G32_SINT": "MTLPixelFormatRG32Sint",
      "DXGI_FORMAT_R32G32_UINT": "MTLPixelFormatRG32Uint",
      "DXGI_FORMAT_R32_FLOAT": "MTLPixelFormatR32Float",
      "DXGI_FORMAT_R32_SINT": "MTLPixelFormatR32Sint",
      "DXGI_FORMAT_R32_UINT": "MTLPixelFormatR32Uint",
      "DXGI_FORMAT_R8G8B8A8_SINT": "MTLPixelFormatRGBA8Sint",
      "DXGI_FORMAT_R8G8B8A8_SNORM": "MTLPixelFormatRGBA8Snorm",
      "DXGI_FORMAT_R8G8B8A8_UINT": "MTLPixelFormatRGBA8Uint",
      "DXGI_FORMAT_R8G8B8A8_UNORM": "MTLPixelFormatRGBA8Unorm",
      "DXGI_FORMAT_R8G8B8A8_UNORM_SRGB": "MTLPixelFormatRGBA8Unorm_sRGB",
      "DXGI_FORMAT_R8_SINT": "MTLPixelFormatR8Sint",
      "DXGI_FORMAT_R8_SNORM": "MTLPixelFormatR8Snorm",
      "DXGI_FORMAT_R8_UINT": "MTLPixelFormatR8Uint",
      "DXGI_FORMAT_R8_UNORM": "MTLPixelFormatR8Unorm",
      "DXGI_FORMAT_UNKNOWN": "MTLPixelFormatInvalid"
    },
    "heap_type_map": {
      "D3D12_HEAP_TYPE_CUSTOM": "MTLStorageModeManaged",
      "D3D12_HEAP_TYPE_DEFAULT": "MTLStorageModePrivate",
      "D3D12_HEAP_TYPE_READBACK": "MTLStorageModeShared",
      "D3D12_HEAP_TYPE_UPLOAD": "MTLStorageModeShared"
    },
    "interfaces": {
      "ID3D12CommandAllocator": {
        "concrete_objc": "(no direct Metal equivalent)",
        "dxmt_class": "MTLD3D12CommandAllocator",
        "dxmt_file": "src/d3d12/d3d12_command_allocator.cpp",
        "metal_protocol": "MTLCommandQueue (for command buffer allocation)",
        "methods": {
          "Reset": {
            "difficulty": "moderate",
            "metal": "Recycle command buffer pool",
            "notes": "Reset allocator for reuse. DXMT resets its internal command pool. Metal command buffers are one-shot (create, commit, done). Recycling is custom.",
            "translation": "reset internal command buffer pool / ring allocator"
          }
        }
      },
      "ID3D12CommandQueue": {
        "concrete_objc": "id<MTLCommandQueue>",
        "dxmt_class": "MTLD3D12CommandQueue",
        "dxmt_file": "src/d3d12/d3d12_command_queue.cpp",
        "metal_protocol": "MTLCommandQueue",
        "methods": {
          "CopyTileMappings": {
            "difficulty": "very hard",
            "metal": "MTL4CommandQueue sparse-buffer mapping copy for native placement-sparse buffers; texture mapping copies remain deferred.",
            "notes": "Copy tile mappings now executes for native placement-sparse buffers and is verified by an independent D3D12 CopyTiles readback; sparse texture mapping copies remain deferred until placement-sparse texture ownership is connected.",
            "translation": "native MTL4 buffer mapping copy for the proven reserved-buffer shape; deferred for sparse textures"
          },
          "ExecuteCommandLists": {
            "difficulty": "very hard",
            "metal": "MTLCommandQueue::commandBuffer -> encode commands -> commit",
            "notes": "Core execution. DXMT records D3D12 commands into its own command buffer format, then replays them into MTLCommandBuffers. Each D3D12 command list becomes one or more MTLCommandBuffers.",
            "translation": "for each cmdList: create MTLCommandBuffer, replay recorded commands, commit()"
          },
          "GetClockCalibration": {
            "difficulty": "moderate",
            "metal": "MTLDevice::sampleTimestamps",
            "notes": "CPU/GPU timestamp correlation. Maps to device->sampleTimestamps(&cpu, &gpu).",
            "translation": "device->sampleTimestamps(&cpuTimestamp, &gpuTimestamp)"
          },
          "GetTimestampFrequency": {
            "difficulty": "moderate",
            "metal": "MTLDevice::counterSets (MTLCommonCounterSetTimestamp) + calibration",
            "notes": "Query GPU timestamp frequency. Metal exposes through counter sets.",
            "translation": "query counter set info for timestamp frequency"
          },
          "Signal": {
            "difficulty": "moderate",
            "metal": "MTLCommandBuffer::encodeSignalEvent / MTLSharedEvent::signal",
            "notes": "GPU signal. Maps to commandBuffer->encodeSignalEvent(event, value).",
            "translation": "commandBuffer->encodeSignalEvent(sharedEvent, fenceValue)"
          },
          "UpdateTileMappings": {
            "difficulty": "very hard",
            "metal": "MTLResourceStateCommandEncoder sparse updates; MTL4CommandQueue placement-sparse updates",
            "notes": "Native 2D RGBA8 reserved resources map and unmap standard tiles through the Metal resource-state encoder, including a two-level 256x256 texture with exact mip-1 readback; reserved buffers use an MTL4 placement-sparse mapping path with a scoped full-buffer fallback and zero-after-unmap semantics. Cross-resource heap-page aliasing and texture heap-page selection remain deferred.",
            "translation": "resourceStateEncoder->updateSparseMappings(texture, tileMappings) for the proven path"
          },
          "Wait": {
            "difficulty": "moderate",
            "metal": "MTLCommandBuffer::encodeWaitForEvent",
            "notes": "GPU wait. Maps to commandBuffer->encodeWaitForEvent(event, value).",
            "translation": "commandBuffer->encodeWaitForEvent(sharedEvent, fenceValue)"
          }
        }
      },
      "ID3D12DescriptorHeap": {
        "concrete_objc": "(no direct Metal equivalent - custom descriptor array)",
        "dxmt_class": "MTLD3D12DescriptorHeap",
        "dxmt_file": "src/d3d12/d3d12_descriptor_heap.cpp",
        "metal_protocol": "N/A",
        "methods": {
          "_internal_GetCPUDescriptorHandle": {
            "difficulty": "easy",
            "metal": "CPU pointer into descriptor array",
            "notes": "Returns CPU-visible handle into descriptor array. DXMT implements as array entry index.",
            "translation": "return heap_base + index * increment_size"
          },
          "_internal_GetGPUDescriptorHandle": {
            "difficulty": "easy",
            "metal": "GPU-visible handle (virtual address)",
            "notes": "Returns GPU-visible handle. DXMT maps to GPU address of backing buffer or direct resource reference.",
            "translation": "return gpu_address_base + index * increment_size"
          }
        }
      },
      "ID3D12Device": {
        "concrete_objc": "id<MTLDevice>",
        "dxmt_class": "MTLD3D12Device",
        "dxmt_file": "src/d3d12/d3d12_device.cpp",
        "metal_protocol": "MTLDevice",
        "methods": {
          "CheckFeatureSupport": {
            "difficulty": "moderate",
            "metal": "MTLDevice properties (supportsFamily:, supportsTextureSampleCount:, etc.)",
            "notes": "No direct equivalent. Feature queries translated to reading MTLDevice capabilities: GPU family, feature set, texture support, etc.",
            "translation": "device->supportsFamily(MTLGPUFamilyAppleX) / multiple property checks"
          },
          "CopyDescriptors": {
            "difficulty": "trivial",
            "metal": "N/A (CPU-side memcpy)",
            "notes": "Copy between descriptor heap ranges. DXMT implements as memcpy of descriptor structs.",
            "translation": "memcpy descriptor entries between heap slots"
          },
          "CopyDescriptorsSimple": {
            "difficulty": "trivial",
            "metal": "N/A (CPU-side memcpy)",
            "notes": "Same as CopyDescriptors but simpler. memcpy.",
            "translation": "memcpy(descriptor_dst, descriptor_src, count * increment_size)"
          },
          "CreateCommandAllocator": {
            "difficulty": "hard",
            "metal": "MTLCommandQueue / MTL4CommandAllocator",
            "notes": "D3D12 command allocators have no direct Metal equivalent. DXMT implements its own command pool/buffer recycling. Metal 4 adds MTL4CommandAllocator.",
            "translation": "custom allocator pool (DXMT: ring buffer allocator)"
          },
          "CreateCommandList": {
            "difficulty": "moderate",
            "metal": "MTLCommandQueue::commandBuffer / MTL4CommandBuffer::beginCommandBufferWithAllocator",
            "notes": "D3D12 immediate/deferred command lists map to MTLCommandBuffer. DXMT uses a deferred recording model that replays into Metal command buffers. Metal 4 adds explicit begin/end.",
            "translation": "commandQueue->commandBuffer() or allocator-based recording"
          },
          "CreateCommandQueue": {
            "difficulty": "moderate",
            "metal": "MTLDevice::newCommandQueue / newCommandQueueWithMaxCommandBufferCount / newCommandQueueWithDescriptor",
            "notes": "D3D12 queue type maps: DIRECT->MTLCommandQueue, COMPUTE->MTLCommandQueue, COPY->MTLCommandQueue (Blit). DXMT wraps in MTLD3D12CommandQueue.",
            "translation": "device->newCommandQueueWithDescriptor(desc)"
          },
          "CreateCommandSignature": {
            "difficulty": "hard",
            "metal": "N/A (custom indirect command format)",
            "notes": "Command signatures define the layout for ExecuteIndirect. Maps to custom indirect argument encoding. Metal has MTLIndirectCommandBuffer for similar functionality.",
            "translation": "parse signature -> build indirect argument layout for MTLIndirectCommandBuffer or custom encoding"
          },
          "CreateCommittedResource": {
            "difficulty": "moderate",
            "metal": "MTLDevice::newBufferWithLength / newTextureWithDescriptor / newHeap + sub-allocation",
            "notes": "Committed = explicit heap. Maps to newBuffer/newTexture directly, or newHeap then sub-allocate. D3D12_HEAP_TYPE_DEFAULT -> private GPU memory, UPLOAD -> shared/managed, READBACK -> shared.",
            "translation": "device->newBufferWithLength(length, options) or device->newTextureWithDescriptor(texDesc)"
          },
          "CreateComputePipelineState": {
            "difficulty": "hard",
            "metal": "MTLDevice::newComputePipelineStateWithFunction / newComputePipelineStateWithDescriptor",
            "notes": "Direct mapping. CS -> MTLComputePipelineState with computeFunction.",
            "translation": "device->newComputePipelineStateWithDescriptor(computeDesc, options, &reflection, &error)"
          },
          "CreateConstantBufferView": {
            "difficulty": "easy",
            "metal": "N/A (implicit via buffer + offset)",
            "notes": "CBVs in Metal are just buffer+offset. DXMT stores GPU address in descriptor heap slot.",
            "translation": "store gpu_address + size in descriptor slot"
          },
          "CreateDepthStencilView": {
            "difficulty": "easy",
            "metal": "N/A (implicit via MTLTexture with pixelFormat depth/stencil in render pass)",
            "notes": "DSVs map to depth attachment in MTLRenderPassDescriptor. Stencil-only DSV maps to stencil aspect.",
            "translation": "store MTLTexture ref + slice/level for depth attachment"
          },
          "CreateDescriptorHeap": {
            "difficulty": "very hard",
            "metal": "N/A (no direct equivalent)",
            "notes": "Metal has no descriptor heap concept. DXMT implements descriptor heaps as CPU-side arrays that map to Metal buffer bindings at draw/dispatch time. CBV_SRV_UAV heaps -> buffer/texture binding arrays. SAMPER heaps -> MTLSamplerState arrays.",
            "translation": "custom descriptor heap (CPU-side allocation, GPU binding at draw time)"
          },
          "CreateFence": {
            "difficulty": "moderate",
            "metal": "MTLDevice::newEvent / newSharedEvent / MTLFence",
            "notes": "D3D12 fence (GPU-CPU sync with value) maps to MTLSharedEvent (if cross-process needed) or MTLEvent. MTLSharedEvent has signalValue / waitUntilScheduledValue. MTLFence is for command buffer ordering only.",
            "translation": "device->newSharedEvent() for cross-process, device->newEvent() for in-process"
          },
          "CreateGraphicsPipelineState": {
            "difficulty": "very hard",
            "metal": "MTLDevice::newRenderPipelineStateWithDescriptor / newRenderPipelineStateWithMeshDescriptor",
            "notes": "D3D12 PSO maps to MTLRenderPipelineState. Shader bytecode (DXIL) must be transpiled to MSL via airconv. VS->vertexFunction, PS->fragmentFunction, GS/HS/DS/MS->mesh/object functions. Blend state, rasterizer state, depth stencil all map to pipeline descriptor fields.",
            "translation": "device->newRenderPipelineStateWithDescriptor(mtlRenderPipelineDesc, &error)"
          },
          "CreateHeap": {
            "difficulty": "moderate",
            "metal": "MTLDevice::newHeapWithDescriptor",
            "notes": "Maps to MTLHeap. D3D12 heaps -> MTLHeap with sub-allocation via newBufferWithLength/newTextureWithDescriptor on the heap.",
            "translation": "device->newHeapWithDescriptor(heapDesc)"
          },
          "CreatePipelineLibrary": {
            "difficulty": "hard",
            "metal": "MTLDevice::newBinaryArchiveWithURL / MTLDynamicLibrary",
            "notes": "Pipeline libraries store compiled PSOs for reuse. Metal has MTLBinaryArchive for pipeline caching and MTLDynamicLibrary for shader code sharing.",
            "translation": "device->newBinaryArchiveWithDescriptor(archiveDesc, &error)"
          },
          "CreatePipelineState": {
            "difficulty": "hard",
            "metal": "MTLDevice::newRenderPipelineStateWithDescriptor / newComputePipelineStateWithDescriptor (stream format)",
            "notes": "Device2 added stream-based PSO creation. Same Metal calls as CreateGraphics/ComputePipelineState.",
            "translation": "same as CreateGraphics/ComputePipelineState but from stream desc"
          },
          "CreatePlacedResource": {
            "difficulty": "moderate",
            "metal": "MTLHeap sub-allocation: MTLHeap::newBufferWithLength / newTextureWithDescriptor:offset:",
            "notes": "Placed buffers retain the existing heap-backed buffer path. Default-heap placed textures use a native Metal placement heap and offset texture allocation when the heap can host textures; a focused overlapping R32_FLOAT alias switches contents through D3D12 aliasing barriers, and recorded copy/barrier resources retain caller-released objects until replay, while broader formats and heap-reuse matrices remain gated.",
            "translation": "heap->newBufferWithLength(length, options, offset) or heap->newTextureWithDescriptor(texDesc, offset)"
          },
          "CreateQueryHeap": {
            "difficulty": "hard",
            "metal": "MTLDevice::newCounterSampleBufferWithDescriptor (for timestamp) / custom query buffer (for occlusion/pipeline stats)",
            "notes": "D3D12 query types: TIMESTAMP->MTLCounterSampleBuffer, OCCLUSION->MTLRenderPassVisibilityResultBuffer, PIPELINE_STATISTICS->custom buffer. Metal doesn't have pipeline statistics natively.",
            "translation": "device->newCounterSampleBufferWithDescriptor(desc) for timestamps, custom buffer for occlusion"
          },
          "CreateRenderTargetView": {
            "difficulty": "easy",
            "metal": "N/A (implicit via MTLTexture slice/level in render pass descriptor)",
            "notes": "RTVs map to MTLTexture attachments in MTLRenderPassDescriptor. May need texture view for format reinterpretation.",
            "translation": "store MTLTexture ref + slice/level for render pass attachment"
          },
          "CreateReservedResource": {
            "difficulty": "very hard",
            "metal": "MTLDevice::newTextureWithDescriptor (with MTLTextureUsageSparseBinding if available) / sparse tile management",
            "notes": "Tiled/reserved resources map to Metal sparse textures (MTLDevice supportsSparseTextures). Tile mapping via MTLResourceStateCommandEncoder or MTLBlitCommandEncoder updateSparseMappings. Not all Metal GPUs support sparse.",
            "translation": "device->newTextureWithDescriptor(sparseDesc) + sparse tile management"
          },
          "CreateRootSignature": {
            "difficulty": "very hard",
            "metal": "MTLArgumentEncoder / MTL4ArgumentTable + manual binding layout",
            "notes": "Root signatures define the binding layout. Metal uses argument encoders (MTL3) or argument tables (MTL4). DXMT parses root signature bytecode and builds its own binding layout that maps root parameters to Metal buffer/texture/sampler slots.",
            "translation": "parse bytecode -> build binding layout -> MTLArgumentEncoder or direct setBuffer/setTexture"
          },
          "CreateSampler": {
            "difficulty": "easy",
            "metal": "MTLDevice::newSamplerStateWithDescriptor",
            "notes": "Direct mapping. D3D12_SAMPLER_DESC -> MTLSamplerDescriptor. Address modes, filter, LOD, comparison, border color all map. Anisotropy maps. D3D12 border color is more limited.",
            "translation": "device->newSamplerStateWithDescriptor(samplerDesc)"
          },
          "CreateShaderResourceView": {
            "difficulty": "moderate",
            "metal": "N/A (implicit via MTLTexture / MTLBuffer + pixel format)",
            "notes": "SRVs map to MTLTexture (for texture SRVs) or MTLBuffer (for buffer SRVs). Format conversion may require creating a texture view with different pixel format. Raw buffers map to buffer+offset.",
            "translation": "create MTLTextureView or store buffer+offset+format in descriptor"
          },
          "CreateSharedHandle": {
            "difficulty": "hard",
            "metal": "Process-local Win32 handle registry; MTLSharedTextureHandle / MTLSharedEvent remain future cross-process paths",
            "notes": "Creates a retained process-local handle for D3D12 child objects, with named lookup and explicit rejection of unknown handles. Cross-process reconstruction remains unsupported because the current MetalSharp bridge has no shared-resource transport.",
            "translation": "retain COM child behind a duplicated event handle and resolve it through the registry"
          },
          "CreateUnorderedAccessView": {
            "difficulty": "hard",
            "metal": "N/A (implicit via MTLTexture with usageShaderWrite / MTLBuffer)",
            "notes": "UAVs map to MTLTexture with usageShaderWrite or MTLBuffer. Atomic UAVs use MTLBuffer with atomic access. Counter UAVs need append/consume emulation.",
            "translation": "create MTLTexture with usageShaderWrite or buffer+offset+atomic_flag"
          },
          "Evict": {
            "difficulty": "easy",
            "metal": "MTLResidencySet::removeResource (implicit on Apple unified memory)",
            "notes": "Valid pageable arrays are accepted as resident-by-default on Apple unified memory; null arrays and null entries return E_INVALIDARG rather than false success.",
            "translation": "validate pageable list; no physical eviction on unified memory"
          },
          "GetCopyableFootprints": {
            "difficulty": "moderate",
            "metal": "N/A (custom calculation)",
            "notes": "Computes layout for texture copy operations. Must be reimplemented based on Metal texture layout rules. Row pitch, slice pitch, offset calculations.",
            "translation": "custom footprint calculation matching D3D12 layout"
          },
          "GetDescriptorHandleIncrementSize": {
            "difficulty": "trivial",
            "metal": "N/A",
            "notes": "Returns sizeof(descriptor) for D3D12. In DXMT: fixed sizes per heap type.",
            "translation": "return fixed size (e.g., 64 for CBV_SRV_UAV, 256 for sampler)"
          },
          "GetDeviceRemovedReason": {
            "difficulty": "trivial",
            "metal": "N/A (no direct equivalent)",
            "notes": "Metal doesn't expose device removal. Return S_OK unless device is actually lost.",
            "translation": "return S_OK (Metal doesn't expose device removal)"
          },
          "GetNodeCount": {
            "difficulty": "trivial",
            "metal": "N/A (single GPU)",
            "notes": "Metal is single-device. Always returns 1.",
            "translation": "return 1;"
          },
          "GetResourceTiling": {
            "difficulty": "moderate",
            "metal": "D3D12 tile-shape calculation paired with Metal sparse texture allocation",
            "notes": "The proven reserved-resource path reports standard tile shape and subresource tilings for 2D RGBA8 resources and a focused buffer compatibility path reports 64 KiB buffer tiles; packed mips, physical heap-page selection, and broader layouts remain gated.",
            "translation": "calculate D3D12 standard tiling from format and resource dimensions"
          },
          "MakeResident": {
            "difficulty": "easy",
            "metal": "MTLResidencySet / implicit Apple unified-memory residency",
            "notes": "Valid pageable arrays are accepted as resident-by-default on Apple unified memory; null arrays and null entries return E_INVALIDARG rather than false success.",
            "translation": "validate pageable list; no physical make-resident transition on unified memory"
          },
          "OpenSharedHandle": {
            "difficulty": "hard",
            "metal": "Process-local retained-object registry",
            "notes": "Resolves a handle created by CreateSharedHandle and QueryInterfaces the retained D3D12 object; unknown handles return DXGI_ERROR_INVALID_CALL.",
            "translation": "lookup retained COM child by duplicated event handle"
          },
          "OpenSharedHandleByName": {
            "difficulty": "very hard",
            "metal": "Process-local named handle registry",
            "notes": "Named lookup duplicates the registry handle and preserves object lifetime. Cross-process named sharing remains unsupported.",
            "translation": "lookup name, duplicate registry handle, and resolve through OpenSharedHandle"
          },
          "SetEventOnMultipleFenceCompletion": {
            "difficulty": "moderate",
            "metal": "MTLSharedEvent::notifyListener",
            "notes": "Multiple fence completion notification. Maps to MTLSharedEvent listener.",
            "translation": "sharedEvent->notifyListener(listener, value)"
          },
          "SetResidencyPriority": {
            "difficulty": "trivial",
            "metal": "N/A",
            "notes": "No Metal equivalent. No-op.",
            "translation": "no-op"
          },
          "SetStablePowerState": {
            "difficulty": "trivial",
            "metal": "N/A",
            "notes": "Developer-only D3D12 feature. No Metal equivalent. Return S_OK.",
            "translation": "no-op (return S_OK)"
          }
        }
      },
      "ID3D12Fence": {
        "concrete_objc": "id<MTLSharedEvent>",
        "dxmt_class": "MTLD3D12Fence",
        "dxmt_file": "src/d3d12/d3d12_fence.cpp",
        "metal_protocol": "MTLSharedEvent",
        "methods": {
          "GetCompletedValue": {
            "difficulty": "trivial",
            "metal": "MTLSharedEvent::signaledValue",
            "notes": "Direct mapping. Returns current signaled value.",
            "translation": "sharedEvent->signaledValue()"
          },
          "SetEventOnCompletion": {
            "difficulty": "moderate",
            "metal": "MTLSharedEvent::notifyListener:atValue",
            "notes": "CPU wait for GPU fence value. Maps to shared event listener notification.",
            "translation": "sharedEvent->notifyListener(listener, value)"
          },
          "Signal": {
            "difficulty": "trivial",
            "metal": "MTLSharedEvent::signal",
            "notes": "CPU-side signal. Maps to sharedEvent->signal(value).",
            "translation": "sharedEvent->signal(value)"
          }
        }
      },
      "ID3D12GraphicsCommandList": {
        "concrete_objc": "id<MTLRenderCommandEncoder> / id<MTLComputeCommandEncoder> / id<MTLBlitCommandEncoder>",
        "dxmt_class": "MTLD3D12GraphicsCommandList",
        "dxmt_file": "src/d3d12/d3d12_command_list.cpp",
        "metal_protocol": "MTLCommandEncoder (polymorphic)",
        "methods": {
          "AtomicCopyBufferUINT": {
            "difficulty": "hard",
            "metal": "Compute shader (atomic exchange via MTLBuffer)",
            "notes": "Atomic buffer copy. Metal has atomic buffer operations in compute shaders.",
            "translation": "dispatch compute shader with atomic operations"
          },
          "AtomicCopyBufferUINT64": {
            "difficulty": "hard",
            "metal": "Compute shader (atomic exchange via MTLBuffer)",
            "notes": "Same as UINT version but 64-bit.",
            "translation": "dispatch compute shader with 64-bit atomic operations"
          },
          "BeginEvent": {
            "difficulty": "trivial",
            "metal": "MTLCommandEncoder::pushDebugGroup",
            "notes": "Direct mapping for GPU debugging.",
            "translation": "encoder->pushDebugGroup(label)"
          },
          "BeginQuery": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder::setVisibilityResultMode",
            "notes": "Occlusion queries map to visibility result. TIMESTAMP queries use MTLCounterSampleBuffer. Begin/End maps to setVisibilityResultMode(MTLVisibilityResultModeCounting, offset).",
            "translation": "renderEncoder->setVisibilityResultMode(MTLVisibilityResultModeCounting, offset)"
          },
          "ClearDepthStencilView": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder (clear in render pass) / MTLBlitCommandEncoder",
            "notes": "Metal clears happen as loadAction=MTLLoadActionClear on render pass attachment. Requires starting a clear-only render pass, OR using blit encoder if available.",
            "translation": "MTLLoadActionClear on depth attachment in render pass"
          },
          "ClearRenderTargetView": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder (clear in render pass)",
            "notes": "Same as DSV clear. Use MTLLoadActionClear on color attachment.",
            "translation": "MTLLoadActionClear on color attachment in render pass"
          },
          "ClearState": {
            "difficulty": "hard",
            "metal": "End and restart encoder",
            "notes": "Metal has no explicit clear-state. Must end current encoder, start fresh.",
            "translation": "end current encoder, reset all bindings, start new encoder"
          },
          "ClearUnorderedAccessViewFloat": {
            "difficulty": "hard",
            "metal": "Compute shader clear (custom)",
            "notes": "Same as Uint version but with float values.",
            "translation": "dispatch compute shader to write float clear value to UAV"
          },
          "ClearUnorderedAccessViewUint": {
            "difficulty": "hard",
            "metal": "Compute shader clear (custom)",
            "notes": "Metal has no native UAV clear. Must dispatch a compute shader that writes the clear value. DXMT implements this with a built-in clear shader.",
            "translation": "dispatch compute shader to write clear value to UAV"
          },
          "Close": {
            "difficulty": "easy",
            "metal": "MTLCommandEncoder::endEncoding",
            "notes": "End current encoder. DXMT tracks recording state.",
            "translation": "currentEncoder->endEncoding()"
          },
          "CopyBufferRegion": {
            "difficulty": "easy",
            "metal": "MTLBlitCommandEncoder::copyFromBuffer:sourceOffset:toBuffer:destinationOffset:size",
            "notes": "Direct mapping. Must open blit encoder if not already active.",
            "translation": "blitEncoder->copyFromBuffer(srcBuffer, srcOffset, dstBuffer, dstOffset, size)"
          },
          "CopyResource": {
            "difficulty": "easy",
            "metal": "MTLBlitCommandEncoder::copyFromTexture / copyFromBuffer",
            "notes": "Full resource copy. Texture->texture or buffer->buffer.",
            "translation": "blitEncoder full resource copy"
          },
          "CopyTextureRegion": {
            "difficulty": "moderate",
            "metal": "MTLBlitCommandEncoder::copyFromTexture:sourceSlice:sourceLevel:sourceOrigin:sourceSize:toTexture:destinationSlice:destinationLevel:destinationOrigin",
            "notes": "Maps to Metal texture copy. Must handle subresource index -> slice+level conversion. Format conversion copies may need compute shader.",
            "translation": "blitEncoder->copyFromTexture(src, srcSlice, srcLevel, srcOrigin, srcSize, dst, dstSlice, dstLevel, dstOrigin)"
          },
          "CopyTiles": {
            "difficulty": "very hard",
            "metal": "MTLBlitCommandEncoder texture/buffer copies over mapped sparse tiles",
            "notes": "The proven 2D RGBA8 path expands a multi-tile D3D12 request into exact 64 KiB placed-footprint copies and verifies map/unmap readback, including a standard two-level mip texture; a focused reserved-buffer path expands tile copies over native MTL4 sparse buffers, with a full compatibility fallback, MTL4 mapping-copy, and MTL4 buffer-copy readback proof; packed/partial mips, boxed regions, cross-resource heap-page aliasing, and physical sparse-texture mappings remain gated.",
            "translation": "expand each standard tile into a buffer/texture blit"
          },
          "DiscardResource": {
            "difficulty": "easy",
            "metal": "MTLTexture::replaceRegion / discard (implicit via MTLLoadActionDontCare)",
            "notes": "Setting loadAction to MTLLoadActionDontCare discards previous contents.",
            "translation": "use MTLLoadActionDontCare on attachment or blit encoder discard"
          },
          "Dispatch": {
            "difficulty": "easy",
            "metal": "MTLComputeCommandEncoder::dispatchThreadgroups",
            "notes": "Direct mapping. D3D12 thread group count -> MTLSize threadgroupCount. Thread group size from PSO.",
            "translation": "computeEncoder->dispatchThreadgroups(threadgroupCount, threadsPerThreadgroup)"
          },
          "DrawIndexedInstanced": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::drawIndexedPrimitives",
            "notes": "Index buffer -> MTLBuffer with indexType (uint16/uint32). baseVertex and baseInstance map directly.",
            "translation": "renderEncoder->drawIndexedPrimitives(primitiveType, indexCount, indexType, indexBuffer, indexBufferOffset, instanceCount, baseVertex, baseInstance)"
          },
          "DrawInstanced": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::drawPrimitives",
            "notes": "D3D12 topology -> MTLPrimitiveType conversion. instance_count maps directly. base_instance maps to startInstance.",
            "translation": "renderEncoder->drawPrimitives(primitiveType, vertexStart, vertexCount, instanceCount, baseInstance)"
          },
          "EndEvent": {
            "difficulty": "trivial",
            "metal": "MTLCommandEncoder::popDebugGroup",
            "notes": "Direct mapping.",
            "translation": "encoder->popDebugGroup()"
          },
          "EndQuery": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder::setVisibilityResultMode (MTLVisibilityResultModeDisabled)",
            "notes": "End visibility counting for occlusion queries.",
            "translation": "renderEncoder->setVisibilityResultMode(MTLVisibilityResultModeDisabled, 0)"
          },
          "ExecuteBundle": {
            "difficulty": "moderate",
            "metal": "Replay recorded bundle commands into current encoder",
            "notes": "No direct Metal equivalent. DXMT bundles are pre-recorded command arrays that get replayed.",
            "translation": "replay bundle commands into current Metal encoder"
          },
          "ExecuteIndirect": {
            "difficulty": "hard",
            "metal": "MTLRenderCommandEncoder::drawPrimitives / drawIndexedPrimitives with indirect buffer / MTLComputeCommandEncoder::dispatchThreadgroupsWithIndirectBuffer",
            "notes": "D3D12 command signature -> Metal indirect draw/dispatch. DXMT parses the signature and either maps to Metal indirect commands or emulates with CPU-side argument parsing.",
            "translation": "renderEncoder->drawPrimitives(indirectBuffer, offset) or dispatchThreadgroupsWithIndirectBuffer"
          },
          "IASetIndexBuffer": {
            "difficulty": "easy",
            "metal": "Store for drawIndexedPrimitives",
            "notes": "Store index buffer ref. Used at drawIndexedPrimitives time. DXGI_FORMAT_R16_UINT -> MTLIndexTypeUInt16, R32 -> MTLIndexTypeUInt32.",
            "translation": "store index buffer + offset + format for next drawIndexedPrimitives call"
          },
          "IASetPrimitiveTopology": {
            "difficulty": "easy",
            "metal": "Store for use at draw time (MTLPrimitiveType)",
            "notes": "D3D12_PRIMITIVE_TOPOLOGY -> MTLPrimitiveType. POINTLIST->MTLPrimitiveTypePoint, TRIANGLELIST->MTLPrimitiveTypeTriangle, etc. Strip topologies need restart index handling.",
            "translation": "store topology -> convert to MTLPrimitiveType at drawPrimitives/drawIndexedPrimitives"
          },
          "IASetVertexBuffers": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setVertexBuffer:offset:atIndex",
            "notes": "Direct mapping. D3D12 slot -> Metal buffer index (typically start at 30 to avoid shader arguments).",
            "translation": "renderEncoder->setVertexBuffer(buffer, offset, startIndex + i) for each buffer"
          },
          "OMSetBlendFactor": {
            "difficulty": "trivial",
            "metal": "MTLRenderCommandEncoder::setBlendColorRed:green:blue:alpha",
            "notes": "Direct mapping.",
            "translation": "renderEncoder->setBlendColorRed(r, g, b, a)"
          },
          "OMSetDepthBounds": {
            "difficulty": "moderate",
            "metal": "fragment-stage comparison against a shader-readable view of the active depth attachment",
            "notes": "Apple M4 rejects the macOS 26 native depth-bounds encoder state. DXMT records OMSetDepthBounds and injects a per-fragment stored-depth comparison only into PSOs whose DEPTH_STENCIL1/2 stream enables DepthBoundsTestEnable.",
            "translation": "bind clamped bounds at fragment buffer 28 plus the active DSV mip/slice view at texture 126, then discard fragments outside the inclusive range"
          },
          "OMSetRenderTargets": {
            "difficulty": "moderate",
            "metal": "MTLRenderPassDescriptor colorAttachments + depthAttachment",
            "notes": "Must create/recreate render pass descriptor with the specified attachments. Up to 8 RTVs + 1 DSV. Metal requires ending/starting a new render pass when targets change.",
            "translation": "build MTLRenderPassDescriptor with colorAttachments[i] and depthAttachment, start new render pass"
          },
          "OMSetStencilRef": {
            "difficulty": "trivial",
            "metal": "MTLRenderCommandEncoder::setStencilFrontReferenceValue:backReferenceValue",
            "notes": "Direct mapping. D3D12 has single stencil ref -> Metal supports separate front/back.",
            "translation": "renderEncoder->setStencilFrontReferenceValue(ref, backRef)"
          },
          "RSSetScissorRects": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setScissorRect:count",
            "notes": "Direct mapping. D3D12 rect -> MTLScissorRect.",
            "translation": "renderEncoder->setScissorRects(scissorRects, count)"
          },
          "RSSetViewports": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setViewport:count",
            "notes": "Direct mapping. D3D12 viewport -> MTLViewport. Metal only supports 1 viewport (no VP array without vertex amplification).",
            "translation": "renderEncoder->setViewports(viewports, count)"
          },
          "Reset": {
            "difficulty": "moderate",
            "metal": "MTLCommandQueue::commandBuffer (new buffer)",
            "notes": "Reset command list for reuse. Get new command buffer from allocator.",
            "translation": "commandBuffer = allocator->newCommandBuffer()"
          },
          "ResolveQueryData": {
            "difficulty": "hard",
            "metal": "MTLBlitCommandEncoder::resolveCounters (Metal 4) / copy from visibility result buffer",
            "notes": "Copy query results to a buffer. Occlusion results from visibility buffer, timestamps from counter sample buffer.",
            "translation": "blitEncoder resolve or copy visibility result buffer to destination"
          },
          "ResolveSubresource": {
            "difficulty": "easy",
            "metal": "MTLBlitCommandEncoder::resolveTexture",
            "notes": "MSAA resolve. Maps directly to Metal resolve operation.",
            "translation": "blitEncoder->resolveTexture(src, slice, level, dst, slice, level)"
          },
          "ResolveSubresourceRegion": {
            "difficulty": "moderate",
            "metal": "MTLBlitCommandEncoder::resolveTexture with region",
            "notes": "Partial resolve. Metal supports region-based resolve.",
            "translation": "blitEncoder->resolveTexture(src, srcSlice, srcLevel, srcOrigin, srcSize, dst, dstSlice, dstLevel, dstOrigin)"
          },
          "ResourceBarrier": {
            "difficulty": "very hard",
            "metal": "N/A (implicit in Metal) / MTLFence / MTLBarrier (Metal 4)",
            "notes": "CRITICAL: Metal has implicit resource barriers within a command buffer. D3D12 UAV barriers need MTLFence or memory barrier. Metal 4 adds explicit barriers via MTL4RenderCommandEncoder::barrier. DXMT currently relies on implicit barriers + careful command buffer splitting.",
            "translation": "mostly no-op (implicit), UAV barriers need MTLFence/barrier, texture layout transitions need render pass splits"
          },
          "SOSetTargets": {
            "difficulty": "very hard",
            "metal": "N/A (no stream output in Metal) / compute emulation",
            "notes": "Metal has no stream output. Must be emulated via transform feedback or compute shader. Most DXMT implementations return NOT_IMPLEMENTED.",
            "translation": "not supported (Metal has no SO) / compute emulation"
          },
          "SetComputeRoot32BitConstant": {
            "difficulty": "easy",
            "metal": "MTLComputeCommandEncoder::setBytes",
            "notes": "Root constant. Maps to setBytes with the constant value at the appropriate offset in the push constant buffer.",
            "translation": "computeEncoder->setBytes(&value, sizeof(uint32_t), bufferIndex + slotOffset)"
          },
          "SetComputeRoot32BitConstants": {
            "difficulty": "easy",
            "metal": "MTLComputeCommandEncoder::setBytes",
            "notes": "Array of root constants. setBytes with the array.",
            "translation": "computeEncoder->setBytes(values, count * sizeof(uint32_t), bufferIndex)"
          },
          "SetComputeRootConstantBufferView": {
            "difficulty": "easy",
            "metal": "MTLComputeCommandEncoder::setBuffer:offset:atIndex",
            "notes": "Root CBV. GPU virtual address -> MTLBuffer + offset.",
            "translation": "computeEncoder->setBuffer(buffer, offset, slotIndex)"
          },
          "SetComputeRootDescriptorTable": {
            "difficulty": "hard",
            "metal": "MTLComputeCommandEncoder::setBuffer / setTexture / setSamplerState",
            "notes": "Root descriptor table points into descriptor heap. DXMT reads descriptors from heap at the given offset and binds the corresponding Metal resources.",
            "translation": "read descriptors from heap[offset..offset+range] -> setBuffer/setTexture/setSamplerState"
          },
          "SetComputeRootShaderResourceView": {
            "difficulty": "moderate",
            "metal": "MTLComputeCommandEncoder::setTexture:atIndex / setBuffer:offset:atIndex",
            "notes": "Root SRV. Texture SRV -> setTexture, Buffer SRV -> setBuffer.",
            "translation": "computeEncoder->setTexture(texture, slice, level, slotIndex) or setBuffer"
          },
          "SetComputeRootSignature": {
            "difficulty": "hard",
            "metal": "MTLArgumentEncoder (bind arguments)",
            "notes": "Maps root signature layout to Metal buffer/texture/sampler bindings. Root constants -> push constants (setBytes). Root descriptors -> setBuffer/setTexture. Descriptor tables -> offset into bound descriptor heap buffer.",
            "translation": "apply binding layout: setBytes for root constants, setBuffer/setTexture for root descriptors"
          },
          "SetComputeRootUnorderedAccessView": {
            "difficulty": "moderate",
            "metal": "MTLComputeCommandEncoder::setTexture:atIndex / setBuffer:offset:atIndex (with read-write)",
            "notes": "Root UAV. Texture UAV -> setTexture (usageShaderWrite), Buffer UAV -> setBuffer.",
            "translation": "computeEncoder->setTexture(texture, slotIndex) or setBuffer(buffer, offset, slotIndex)"
          },
          "SetDescriptorHeaps": {
            "difficulty": "easy",
            "metal": "N/A (binding happens per-draw)",
            "notes": "Select which descriptor heaps are visible. DXMT stores reference for subsequent root signature binding.",
            "translation": "store heap references for later binding resolution"
          },
          "SetGraphicsRoot32BitConstant": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setBytes",
            "notes": "Same as compute version.",
            "translation": "renderEncoder->setBytes(&value, sizeof(uint32_t), bufferIndex + slotOffset)"
          },
          "SetGraphicsRoot32BitConstants": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setBytes",
            "notes": "Same as compute version.",
            "translation": "renderEncoder->setBytes(values, count * sizeof(uint32_t), bufferIndex)"
          },
          "SetGraphicsRootConstantBufferView": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setBuffer:offset:atIndex",
            "notes": "Same as compute version.",
            "translation": "renderEncoder->setBuffer(buffer, offset, slotIndex)"
          },
          "SetGraphicsRootDescriptorTable": {
            "difficulty": "hard",
            "metal": "MTLRenderCommandEncoder::setBuffer / setTexture / setSamplerState",
            "notes": "Same as compute version but for render encoder.",
            "translation": "same as compute version for render encoder"
          },
          "SetGraphicsRootShaderResourceView": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder::setTexture:atIndex / setBuffer:offset:atIndex",
            "notes": "Same as compute version.",
            "translation": "same as compute for render encoder"
          },
          "SetGraphicsRootSignature": {
            "difficulty": "hard",
            "metal": "MTLArgumentEncoder (bind arguments)",
            "notes": "Same as SetComputeRootSignature but for render pipeline.",
            "translation": "same as SetComputeRootSignature for render encoder"
          },
          "SetGraphicsRootUnorderedAccessView": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder::setTexture:atIndex / setBuffer:offset:atIndex",
            "notes": "Same as compute version.",
            "translation": "same as compute for render encoder"
          },
          "SetMarker": {
            "difficulty": "trivial",
            "metal": "MTLCommandEncoder::insertDebugSignpost",
            "notes": "Direct mapping for GPU debugging.",
            "translation": "encoder->insertDebugSignpost(label)"
          },
          "SetPipelineState": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setRenderPipelineState / MTLComputeCommandEncoder::setComputePipelineState",
            "notes": "Bind pre-compiled pipeline. DXMT stores MTLRenderPipelineState or MTLComputePipelineState in its PSO wrapper.",
            "translation": "renderEncoder->setRenderPipelineState(pso) or computeEncoder->setComputePipelineState(pso)"
          },
          "SetPredication": {
            "difficulty": "very hard",
            "metal": "N/A / compute emulation",
            "notes": "Metal has no native predication. Can be emulated with conditional dispatch or by checking the predicate value in a compute shader.",
            "translation": "emulate with conditional logic or indirect draw/dispatch with count=0"
          },
          "SetSamplePositions": {
            "difficulty": "easy",
            "metal": "MTLRenderCommandEncoder::setSamplePositions (Metal 2.1+)",
            "notes": "Programmable sample positions. Direct mapping.",
            "translation": "renderEncoder->setSamplePositions(positions, count)"
          },
          "SetViewInstanceMask": {
            "difficulty": "moderate",
            "metal": "MTLRenderCommandEncoder vertex amplification",
            "notes": "View instance mask -> Metal vertex amplification with view mask.",
            "translation": "renderEncoder->setVertexAmplificationCount(count, viewMappings)"
          },
          "WriteBufferImmediate": {
            "difficulty": "moderate",
            "metal": "MTLBlitCommandEncoder::fillBuffer / custom compute write",
            "notes": "Write immediate values to buffer. Can use fillBuffer for zeros or compute shader for arbitrary values.",
            "translation": "blitEncoder->fillBuffer(buffer, range, value) or compute shader"
          }
        }
      },
      "ID3D12PipelineState": {
        "concrete_objc": "id<MTLRenderPipelineState> / id<MTLComputePipelineState>",
        "dxmt_class": "MTLD3D12PipelineState",
        "dxmt_file": "src/d3d12/d3d12_pipeline_state.cpp",
        "metal_protocol": "MTLRenderPipelineState / MTLComputePipelineState",
        "methods": {
          "GetCachedBlob": {
            "difficulty": "moderate",
            "metal": "MTLBinaryArchive serialized data",
            "notes": "Return cached pipeline data. Metal uses binary archives for caching.",
            "translation": "return serialized binary archive data"
          }
        }
      },
      "ID3D12Resource": {
        "concrete_objc": "id<MTLBuffer> / id<MTLTexture>",
        "dxmt_class": "MTLD3D12Resource",
        "dxmt_file": "src/d3d12/d3d12_resource.cpp",
        "metal_protocol": "MTLResource (MTLBuffer/MTLTexture)",
        "methods": {
          "GetGPUVirtualAddress": {
            "difficulty": "easy",
            "metal": "MTLBuffer::gpuAddress / MTLTexture::gpuResourceID",
            "notes": "Returns GPU-addressable pointer. Metal buffers have gpuAddress. DXMT maintains its own GPU address space for resource lookup.",
            "translation": "buffer->gpuAddress() + offset"
          },
          "GetHeapProperties": {
            "difficulty": "easy",
            "metal": "MTLHeap::heapType / resource properties",
            "notes": "Query which heap and type. Maps to Metal heap properties.",
            "translation": "return stored heap type and CPU page property"
          },
          "Map": {
            "difficulty": "moderate",
            "metal": "MTLBuffer::contents (shared) / MTLBuffer didModifyRange (managed)",
            "notes": "CPU access to GPU buffer. Shared buffers -> contents() directly. Managed buffers -> contents() + didModifyRange. Private buffers -> must use blit encoder to copy from staging.",
            "translation": "buffer->contents() for shared/managed, staging buffer copy for private"
          },
          "ReadFromSubresource": {
            "difficulty": "moderate",
            "metal": "MTLTexture::getBytes:...fromRegion:... / MTLBlitCommandEncoder",
            "notes": "CPU read from texture. Maps to getBytes for readable textures or blit copy to staging.",
            "translation": "texture->getBytes(bytes, bytesPerRow, bytesPerImage, region, slice, level)"
          },
          "Unmap": {
            "difficulty": "easy",
            "metal": "MTLBuffer::didModifyRange (managed) / no-op (shared)",
            "notes": "Flush CPU writes. Managed buffers need didModifyRange. Shared buffers are coherent.",
            "translation": "buffer->didModifyRange(range) for managed, no-op for shared"
          },
          "WriteToSubresource": {
            "difficulty": "moderate",
            "metal": "MTLTexture::replaceRegion:...withBytes:... / MTLBlitCommandEncoder",
            "notes": "CPU write to texture subresource. Maps to replaceRegion for staging or blit copy.",
            "translation": "texture->replaceRegion(region, slice, level, pixels, bytesPerRow, bytesPerImage)"
          }
        }
      },
      "ID3D12RootSignature": {
        "concrete_objc": "(no direct Metal equivalent - custom binding layout)",
        "dxmt_class": "MTLD3D12RootSignature",
        "dxmt_file": "src/d3d12/d3d12_root_signature.cpp",
        "metal_protocol": "MTLArgumentEncoder / MTL4ArgumentTable",
        "methods": {
          "_internal_ParseRootSignature": {
            "difficulty": "very hard",
            "metal": "Build MTLArgumentEncoder layout or argument table bindings",
            "notes": "Root signature bytecode is parsed into a binding layout. Root constants -> setBytes, root descriptors -> setBuffer/setTexture, descriptor tables -> offset into heap buffer. MTL4 uses argument tables for explicit binding.",
            "translation": "parse bytecode -> build binding layout -> argument encoder or direct binding slots"
          }
        }
      },
      "IDXGISwapChain": {
        "concrete_objc": "CAMetalLayer / MTKView",
        "dxmt_class": "MTLD3D12SwapChain",
        "dxmt_file": "src/d3d12/d3d12_swapchain.cpp",
        "metal_protocol": "CAMetalLayer",
        "methods": {
          "GetBuffer": {
            "difficulty": "easy",
            "metal": "MTLTexture from CAMetalLayer nextDrawable",
            "notes": "Get back buffer at index. DXMT maintains a texture array.",
            "translation": "return swapchain texture at index"
          },
          "Present": {
            "difficulty": "easy",
            "metal": "CAMetalLayer::presentDrawable / MTLDrawable::present",
            "notes": "Present frame. Maps to drawable->present(). VSync via CAMetalLayer.displaySyncEnabled.",
            "translation": "[drawable present] or drawable->presentAtTime(nextVBlank)"
          },
          "ResizeBuffers": {
            "difficulty": "moderate",
            "metal": "Recreate MTLTexture array for CAMetalLayer",
            "notes": "Resize back buffers. Metal: recreate textures matching new size.",
            "translation": "recreate MTLTexture array with new dimensions"
          }
        }
      }
    },
    "topology_map": {
      "D3D_PRIMITIVE_TOPOLOGY_1_CONTROL_POINT_PATCHLIST": "MTLPrimitiveTypeTriangle (tessellation -> mesh shader)",
      "D3D_PRIMITIVE_TOPOLOGY_2_CONTROL_POINT_PATCHLIST": "mesh shader emulation",
      "D3D_PRIMITIVE_TOPOLOGY_3_CONTROL_POINT_PATCHLIST": "mesh shader emulation",
      "D3D_PRIMITIVE_TOPOLOGY_4_CONTROL_POINT_PATCHLIST": "mesh shader emulation",
      "D3D_PRIMITIVE_TOPOLOGY_LINELIST": "MTLPrimitiveTypeLine",
      "D3D_PRIMITIVE_TOPOLOGY_LINELIST_ADJ": "MTLPrimitiveTypeLine (no adjacency in Metal)",
      "D3D_PRIMITIVE_TOPOLOGY_LINESTRIP": "MTLPrimitiveTypeLineStrip",
      "D3D_PRIMITIVE_TOPOLOGY_LINESTRIP_ADJ": "MTLPrimitiveTypeLineStrip (no adjacency)",
      "D3D_PRIMITIVE_TOPOLOGY_POINTLIST": "MTLPrimitiveTypePoint",
      "D3D_PRIMITIVE_TOPOLOGY_TRIANGLELIST": "MTLPrimitiveTypeTriangle",
      "D3D_PRIMITIVE_TOPOLOGY_TRIANGLELIST_ADJ": "MTLPrimitiveTypeTriangle (no adjacency)",
      "D3D_PRIMITIVE_TOPOLOGY_TRIANGLESTRIP": "MTLPrimitiveTypeTriangleStrip",
      "D3D_PRIMITIVE_TOPOLOGY_TRIANGLESTRIP_ADJ": "MTLPrimitiveTypeTriangleStrip (no adjacency)",
      "D3D_PRIMITIVE_TOPOLOGY_UNDEFINED": "N/A"
    }
  },
  "evidence": [
    {
      "kind": "source_map",
      "note": "Generated from DXMT source analysis and macOS Metal SDK headers.",
      "path": "/Volumes/AverySSD/metalsharp/metal-api-table/final/d3d12_to_metal_map.json"
    }
  ],
  "schema": "metalsharp.d3d12-metal.contract.v1",
  "source": {
    "kind": "generated_d3d12_to_metal_map",
    "path": "/Volumes/AverySSD/metalsharp/metal-api-table/final/d3d12_to_metal_map.json",
    "sha256": "f8331e59e08ac45226aa2f60c6f50403ceea06ff2a51cf4d2b273976d9e694de"
  },
  "summary": {
    "difficulty_counts": {
      "easy": 36,
      "hard": 20,
      "moderate": 37,
      "trivial": 14,
      "very hard": 13
    },
    "format_count": 54,
    "heap_type_count": 4,
    "interface_count": 10,
    "method_count": 120,
    "topology_count": 14
  }
}
